# Frontend

前端工作流默认优先于后端工作流。

## 默认顺序

1. 明确页面入口与目标用户
2. 完成页面结构、交互流程、空态/错态、接口占位
3. 通过手动验收确认前端边界
4. 只有在边界稳定后才进入后端实现

## 默认规则

- 前端目录默认放在 `apps/web`
- 前端任务默认优先使用 `frontend-dev`
- UI、状态、数据适配和业务规则不要混在同一文件
- 当单文件接近 300 行时，评估拆分

## 需要回写文档的前端结论

- 关键页面入口
- 核心交互路径
- 主要错误态和恢复方式
- 与后端冻结的接口边界
