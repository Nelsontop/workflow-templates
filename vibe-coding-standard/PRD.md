# PRD Template

## 生成约定

- 首稿应先锁定用户价值、范围边界、版本取舍和验收口径
- PRD 先回答“为什么做、为谁做、如何判断做成了”，再回答“具体做什么”
- 如果产品高度依赖视觉和交互，需补充界面与交互层面的需求约束
- 最终内容必须沉淀回本文件，不能只停留在聊天记录
- 未写入本文件的功能，默认不进入当前版本
- 如果项目采用多前后端结构，默认按 `apps/web` 与 `apps/server` 拆分，并在需求上明确两端边界
- 默认开发顺序是先前端、后后端；后端开发以前端页面、交互和接口边界确认完成为前提
- 如果存在关键用户流程、后台流程或高风险失败分支，应确保能映射到 `ARCH.md` 中对应的核心工作流与验证方式

## 交付物技能参考

| 技能 | 适用交付物 | 适用场景 |
| --- | --- | --- |
| `frontend-dev` | Web 页面、营销页、产品页、仪表盘 | 页面需要强视觉表现、动效、媒体资产或转化文案时优先使用，也是默认前端技能入口 |
| `gif-sticker-maker` | GIF 贴纸、表情包、头像动画 | 需要把人物、宠物、物体或 logo 转成 4 张带文案的动态贴纸时使用 |
| `minimax-docx` | Word / `.docx` 正式文档 | 需要产出、编辑、套模板或排版报告、方案、合同、公文等正式文档时使用 |
| `minimax-pdf` | 高视觉质量 PDF | 需要生成、补全表单或重排设计感较强、可打印的 PDF 时使用 |
| `minimax-xlsx` | Excel / `.xlsx` / `.csv` / `.tsv` | 需要创建、读取、分析、修改、校验电子表格、财务模型或透视表时使用 |
| `pptx-generator` | PowerPoint / `.pptx` 演示文稿 | 需要创建、编辑、读取幻灯片、汇报材料或演示 deck 时使用 |

---

## 产品概述

- 产品名称：
- 一句话定义：
- 目标用户：
- 目标用户当前如何解决这个问题：

## 背景与问题

- 我们要解决的核心问题是什么：
- 这个问题为什么现在必须解决：
- 如果这次不做，代价是什么：

### 证据

- 用户研究：
- 行为数据：
- 支持/销售/运营信号：
- 竞品或市场信号：

## 本期目标与成功指标

| 目标 | 衡量指标 | 当前基线 | 目标值 | 观察窗口 |
| --- | --- | --- | --- | --- |
| 示例：提升核心流程完成率 | 示例：完成率 | 示例：32% | 示例：50% | 示例：上线后 30 天 |

补充说明：

- 本期要完成什么：
- 本期是否只做前端切片：
- 若后端进入本期，前端已确认的范围是什么：

## 非目标

- 本期明确不做什么：
- 哪些用户群或场景不覆盖：
- 哪些平台、端能力或高级配置不进入本期：

## 用户与使用场景

### 主要用户

| 角色 | 当前处境 | 关键诉求 | 为什么优先 |
| --- | --- | --- | --- |
| 示例用户 | 示例处境 | 示例诉求 | 示例原因 |

### 典型使用场景

| 场景 | 触发条件 | 用户目标 | 成功结果 |
| --- | --- | --- | --- |
| 示例场景 | 示例触发 | 示例目标 | 示例结果 |

## 核心用户流程

### 流程 1：主流程名称

1. 用户在什么前提下进入流程
2. 用户在什么页面或界面执行什么动作
3. 系统返回什么反馈
4. 用户最终得到什么结果

失败分支：

- 哪一步可能失败：
- 失败时如何提示：
- 用户下一步可以做什么：

补充要求：

- 每条核心用户流程都应能映射到 `ARCH.md` 中至少一个核心工作流
- 若流程涉及等待、异步处理、外部服务或人工介入，需在 PRD 先写清用户预期结果
- 若流程失败后允许重试、撤销或回退，需写清用户可执行动作

## 功能范围

| 功能 | 是否本期必做 | 优先级 | 依赖项 | 验收口径 |
| --- | --- | --- | --- | --- |
| 示例功能 | 是 | P0 | 无 | 写清触发条件、结果与验收方式 |

## 页面与交互范围

| 页面/界面 | 对应用户/场景 | 主要内容 | 主要动作 | 本期是否必做 |
| --- | --- | --- | --- | --- |
| 示例页面 | 示例用户 | 示例内容 | 示例动作 | 是 |

## 前后端范围拆分

| 端 | 本期范围 | 明确不做 |
| --- | --- | --- |
| 前端 | 示例：页面、交互、空态、错态、接口占位 | 示例：真实持久化、后台任务 |
| 后端 | 示例：确认前端无误后再实现 API 与存储 | 示例：前端未确认前提前扩展接口 |

## 默认目录骨架

```text
your-project/
├── PRD.md
├── ARCH.md
├── project_state.md
├── docker/
│   ├── web.Dockerfile
│   ├── server.Dockerfile
│   └── compose/
│       └── docker-compose.yml
├── apps/
│   ├── web/
│   └── server/
└── reference/
```

## 数据与业务规则

| 实体 | 字段 | 业务含义 | 约束/校验 |
| --- | --- | --- | --- |
| 示例实体 | `name` | 示例含义 | 必填，最多 50 字符 |

补充规则：

- 哪些字段用户可编辑：
- 哪些数据只读：
- 哪些规则会影响结果计算、统计或计费：

## 错误与边界状态

- 网络失败时如何处理：
- 无数据时如何处理：
- 权限不足时如何处理：
- 加载中如何展示：
- 超出范围或非法输入时如何提示：

## 开放问题

| 问题 | 为什么未定 | 负责人 | 需要在何时前解决 |
| --- | --- | --- | --- |
| 示例问题 | 示例原因 | 示例负责人 | 示例时间点 |

## 验收清单

### 目标验收

- [ ] 成功指标已定义，且可在上线后被观测
- [ ] 非目标已明确写出，避免临时扩 scope

### 用户流程验收

- [ ] 核心用户流程可从入口一路走到结果页
- [ ] 失败、空态、权限不足和加载中反馈都已定义
- [ ] 关键用户流程已能映射到 `ARCH.md` 的核心工作流与失败恢复说明

### 切片验收

- [ ] 前端切片已按页面入口完成验收
- [ ] 如进入后端，前端确认结果已记录并作为后端边界输入
- [ ] 当前切片的验证方式已在 `ARCH.md` 中写明入口、预期结果和触发时机

## 发布与回滚

- 计划如何发布：
- 是否灰度：
- 触发回滚的条件是什么：
- 回滚后用户侧预期表现是什么：
- 回滚后哪些数据或状态可能受影响：
- 发布后重点观察哪些指标：

## 附录

- 相关设计稿：
- 相关数据看板：
- 相关用户访谈或工单：
- 相关外部资料：
