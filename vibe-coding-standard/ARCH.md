# ARCH Template

## 生成约定

- 首稿应先锁定技术边界、模块职责、数据流、验证方式和主要风险
- 如果需求范围还不稳定，应先收敛范围，再落架构
- 先回答“为什么这样设计、放弃了什么、以后如何演进”，再回答“目录怎么放、接口怎么写”
- 最终内容必须沉淀回本文件，不能只停留在聊天记录
- 未写入本文件的重大技术决策，默认不直接实施
- 回归测试与生产前验证方案，也要在本文件中明确写清触发时机和执行方式
- 多前后端项目默认采用前后端独立目录，不把两端代码混放在同一 `src/`
- 默认启动方式使用 Docker，默认数据库使用 SQLite；若偏离默认值，需写出原因和迁移成本
- 默认开发顺序是先前端后后端，后端切片应建立在已确认的前端边界之上
- 如果一个工作流存在于代码、部署脚本或数据生命周期中，就应在本文件中留下入口、状态和失败恢复说明

## 实现技能参考

| 技能 | 适用实现形态 | 适用场景 |
| --- | --- | --- |
| `frontend-dev` | Web 前端 | 需要构建高质量页面、动效系统、媒体资产集成和强视觉表达的前端项目时优先使用，也是默认前端技能入口 |
| `fullstack-dev` | 全栈应用 | 需要同时设计后端架构与前后端集成，包括 API、认证、配置、错误处理、文件上传或实时能力时优先使用，也是默认后端技能入口 |
| `android-native-dev` | Android 原生应用 | 需要基于 Kotlin / Compose、Material Design 3、无障碍和构建配置开发 Android 应用时使用 |
| `ios-application-dev` | iOS 应用 | 需要基于 UIKit、SnapKit 或 SwiftUI 构建界面，并遵循 Apple HIG、Dynamic Type、Dark Mode 和无障碍规范时使用 |
| `shader-dev` | 实时图形 / GLSL 效果 | 需要实现 ShaderToy 兼容的 GLSL 效果、程序化图形、粒子、流体或后处理效果时使用 |

---

## 技术目标

- 当前阶段：
- 优先级：
- 主要技术风险：
- 本期主要服务的 PRD 目标：
- 本期明确不支撑的 PRD 非目标：

## 用户范围与规模

- 自用 / 小范围内测 / 公开上线：
- 预估并发用户数：
- 预估数据规模：
- 峰值场景或流量假设：

## 架构选型与取舍

### 总体架构模式

- 推荐模式：单体 / 模块化单体 / 前后端分离 / 事件驱动 / 其他
- 为什么选它：
- 当前阶段不选其他模式的原因：
- 未来可能触发演进的信号：

### 关键技术决策（ADR）

| 决策项 | 可选方案 | 当前选择 | 选择原因 | 代价/放弃了什么 | 何时需要重审 |
| --- | --- | --- | --- | --- | --- |
| 示例：本地开发与联调入口 | 裸机脚本 / Docker Compose | Docker Compose | 降低环境差异 | 启动更重、需要容器知识 | 多人协作或 CI 变慢时 |

补充要求：

- 每个高影响决策都要写清楚 trade-off，而不是只写“最佳实践”
- 如果决策不可逆或迁移成本高，必须写出回退难点
- 如果当前只是阶段性方案，要写出未来替换条件

## 技术栈

- 客户端：
- 服务端：
- 数据库：默认 SQLite，如需替换需在本文件说明
- 部署平台：默认 Docker
- 第三方服务：
- 配置与密钥管理：
- 异步任务/队列（如有）：
- 文件存储（如有）：

## 运行时拓扑与部署约束

- 运行进程/容器：
- 服务间依赖顺序：
- 对外暴露端口：
- 内部网络与服务发现方式：
- 持久化卷或数据目录：
- 健康检查方式：
- 环境变量分层：本地 / 测试 / 生产
- 备份与恢复策略：
- 第三方服务故障时的降级策略：

## 目录与分层

```text
your-project/
├── PRD.md
├── ARCH.md
├── project_state.md
├── docker/
│   ├── web.Dockerfile
│   ├── server.Dockerfile
│   └── compose/
│       └── docker-compose.yml
├── apps/
│   ├── web/
│   │   ├── src/
│   │   ├── public/
│   │   ├── package.json
│   │   └── Dockerfile
│   └── server/
│       ├── src/
│       ├── prisma/
│       ├── package.json
│       └── Dockerfile
├── reference/
│   ├── agency/
│   │   ├── README.md
│   │   ├── build_index.py
│   │   ├── index.json
│   │   └── upstream/
```

分层规则：

- 本项目采用：`feature-first` / `controller-service-repository` / 其他
- 选择原因：
- 允许例外的条件：
- `apps/web` 中的界面层只处理展示和交互
- `apps/web` 中的状态层只处理状态流转
- `apps/web` 中的服务层只处理接口通信
- `apps/server` 中按 controller/service/repository 或 feature-first 分层
- 业务规则不要直接散落在页面组件里，也不要混进数据库访问层

## 模块边界

| 模块 | 职责 | 不负责什么 |
| --- | --- | --- |
| auth | 登录、鉴权态 | 业务数据查询 |

边界检查：

- 哪些模块允许直接依赖数据库层：
- 哪些模块只能通过服务接口调用：
- 哪些共享能力可以沉淀到 `reference/`：
- 哪些能力暂时不抽象，避免过早复用：

## 数据模型

| 实体 | 关键字段 | 关系 |
| --- | --- | --- |
| user | `id`, `email` | 与业务实体的关联 |

补充要求：

- 写清主键策略、唯一约束、软删/硬删策略
- 写清哪些字段由系统生成，哪些字段允许用户输入
- 若有状态字段，需与“状态与失败恢复矩阵”一致

## 核心工作流清单

| 工作流 | 触发入口 | 主要参与方 | 成功结果 | 失败结果 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| 示例：用户注册 | `/signup` 页面提交 | 用户、Web、Server、DB、邮件服务 | 账户创建并进入待验证态 | 返回明确错误并保留输入 | Draft |

补充要求：

- 至少覆盖核心用户流程、后台任务、部署/初始化、数据清理四类工作流
- 若工作流已存在于代码或运维流程中但未定义，状态写 `Missing`
- 每个工作流都应能映射回 PRD 中至少一个用户流程或运营流程

## 工作流状态与失败恢复矩阵

| 工作流/实体 | 状态 | 进入条件 | 用户可见表现 | 系统动作 | 失败/超时后的恢复方式 |
| --- | --- | --- | --- | --- | --- |
| 示例：注册流程 | `pending_verification` | 注册成功待验证 | 提示“请查收邮件” | 生成 token 并发送邮件 | 可重发邮件，超过时限后失效 |

补充要求：

- 至少覆盖成功、校验失败、超时、部分失败、并发冲突五类情况
- 写清用户看到什么、操作者看到什么、日志里应出现什么关键信号
- 若失败不可自动恢复，必须写明人工处理入口

## 系统边界与 Handoff 契约

```text
HANDOFF: [From] -> [To]
  TRIGGER: [由什么动作或事件触发]
  PAYLOAD: { field: type, ... }
  SUCCESS: { field: type, ... }
  FAILURE: { code: string, message: string, retryable: boolean }
  TIMEOUT: [例如 5s / 30s]
  ON_FAILURE: [重试 / 补偿 / 人工介入 / 回滚]
```

要求：

- 至少列出 Web -> Server、Server -> DB、Server -> 第三方 三类边界
- 如前端先行开发，需先冻结占位契约和错误结构，再进入后端实现
- 不允许把“由调用方自己理解”当作契约说明

## 客户端与服务端边界

- 哪些逻辑必须在服务端：
- 哪些数据不能信任前端传入：
- 哪些接口必须鉴权：
- 前端确认后才能冻结哪些接口契约：
- 哪些操作必须幂等：
- 哪些流程需要后台异步处理：
- 哪些逻辑允许前端做乐观更新，哪些不允许：

## 认证、授权与安全

- 认证方式：Session / JWT / OAuth / 其他
- 会话或 Token 生命周期：
- 权限模型：角色 / 资源 / 租户 / 其他
- 哪些接口需要限流：
- 敏感字段如何脱敏：
- 密钥与凭据放在哪里：
- 是否记录审计日志：
- 安全相关失败的默认响应方式：
- 第三方回调或 webhook 如何验签：

## API 设计约束

- 返回格式：
- 错误码约定：
- 分页约定：
- 幂等键或去重策略：
- 超时约定：
- 版本策略：
- 向后兼容策略：

## 状态管理方案

- 全局状态：
- 页面局部状态：
- 缓存策略：
- 接口缓存失效条件：
- 乐观更新与回滚策略：

## 非功能约束

- 性能上限：
- 成本上限：
- 安全底线：
- 可用性目标：
- 可维护性目标：
- 观测性目标：

## 合规与数据治理

- 数据是否包含个人信息：
- 是否支持用户删除数据：
- 是否有地域合规要求：
- 数据保留周期：
- 导出/删除请求如何执行：

## 监控与排错

- 日志位置：
- 关键指标：
- 报错排查原则：
- Trace / Request ID 策略：
- 告警阈值：
- 值班或人工介入方式：

## 测试与验证策略

| 验证类型 | 入口/命令 | 执行环境 | 预期结果 | 触发时机 | 失败后处理 |
| --- | --- | --- | --- | --- | --- |
| lint | 示例：`npm run lint` | 本地 / CI | 无阻塞错误 | 每次切片完成后 | 修复后重跑 |
| typecheck | 示例：`npm run typecheck` | 本地 / CI | 类型通过 | 接口或状态修改后 | 修复类型问题 |
| unit/integration | 示例：`npm test` | 本地 / CI | 核心路径通过 | 功能完成后 | 先最小复现再修复 |
| regression | 示例：按 PRD 入口手测 | 本地预发环境 | 主流程打通 | 提交前 / 发布前 | 记录回归并阻塞发布 |
| smoke test | 示例：真实入口路径检查 | 预发 / 生产前 | 关键结果页可达 | 上线前 | 失败则暂停发布 |
| visual review | 示例：对照设计稿检查 | 本地 / 预发 | 布局、层级、反馈正常 | UI 改动后 | 回退样式或补修 |

补充要求：

- 每个核心工作流至少对应一条验证记录
- 回归测试必须写清入口路径、操作步骤和预期结果
- 若当前阶段没有自动化测试，必须写明缺失项和补齐计划
- 后端开始前，必须记录前端已确认的页面、状态和接口占位范围

## 发布与回滚

- 发布方式：
- 回滚方式：
- 稳定版本判定标准：
- 发布前检查清单：
- 灰度/分批策略（如有）：
- 数据库或配置变更的回滚限制：
- 发布后重点观察指标：

## 假设、未决项与风险跟踪

### 当前假设

- 假设 1：
- 假设 2：

### 未决问题

| 问题 | 影响范围 | 何时必须决定 | 决定人 |
| --- | --- | --- | --- |
| 示例问题 | 影响 API 冻结 | 后端开始前 | 产品/架构负责人 |

### 主要风险与缓解

| 风险 | 触发信号 | 影响 | 缓解方案 |
| --- | --- | --- | --- |
| 示例：前端边界未稳定就进入后端 | 接口频繁改动 | 返工与回归风险升高 | 先冻结交互和占位契约 |

## 默认架构约定

### 前后端隔离

- 默认使用 `apps/web` 承载前端代码，`apps/server` 承载后端代码
- 两端构建、依赖、镜像和运行入口相互独立
- 共享约定放在 `reference/`，而不是通过跨目录直接引用业务代码实现“伪共享”

### 启动方式

- 默认使用 Docker 作为本地启动与联调入口
- 文档、脚本和验收说明应优先给出容器化命令
- 若允许裸机启动，只能作为补充路径，不能替代默认路径

### 数据库默认值

- 默认数据库使用 SQLite
- 适用于模板初期、本地开发和小规模验证
- 若切换到 PostgreSQL、MySQL 等，需要明确触发条件、迁移策略和新增运维成本

### 开发顺序

- 先实现并确认前端切片
- 前端阶段先明确页面结构、状态流转、错误反馈和接口契约占位
- 后端阶段再实现 API、业务规则和 SQLite 持久化
- 如果前端需求未稳定就先做后端，最容易造成接口返工；这是默认不推荐方案
- 若必须先做后端，需在“关键技术决策（ADR）”中写明理由、假设和回退方案

### 外部参考索引

- 可在 `reference/agency` 保存外部 agent Markdown 快照与索引，作为本地技能之外的后备知识源
- 该索引的优先级低于本地技能和项目文档
- 若请求已明确命中 `frontend-dev`、`fullstack-dev` 等本地技能，则不再额外加载同领域 agency 文档
- 动态加载的 agency 文档只用于当前任务，不作为长期持久上下文
